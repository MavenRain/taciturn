-- the ZKMARK keyword pair of D-M0-3 beside a plain def of the same name
-- shape, so the flag is the only difference
zk def square : (w x : Field p) -> Field p := fun (w x : Field p) => mul x x
def plain : (x : Field p) -> Field p := fun (x : Field p) => mul x x
