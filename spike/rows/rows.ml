(* rows.ml.  The entry module: dune names the executable after it, so the
   build lands _build/default/spike/rows/rows.exe.  The whole driver sits
   in main.ml, as the brief section 3.2 asks. *)

let () = Main.run ()
